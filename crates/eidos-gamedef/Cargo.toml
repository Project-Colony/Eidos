[package]
name = "eidos-gamedef"
version.workspace = true
description = "Eidos declarative per-game definitions (modelled on MO2's IPluginGame schema)"
license.workspace = true
repository.workspace = true
edition.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
toml = "1"
