[package]
name = "eidos-instance"
version = "0.0.0"
description = "Eidos instance model: global vs portable, mods/overwrite layout, load order"
license.workspace = true
repository.workspace = true
edition.workspace = true

[dependencies]
eidos-ini = { path = "../eidos-ini" }
