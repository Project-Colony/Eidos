[package]
name = "eidos-addons"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
eidos-paths = { path = "../eidos-paths" }
serde = { version = "1", features = ["derive"] }
toml = "1"
